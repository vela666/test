// import Cookies from 'js-cookie'

export const localTokenNameKey = 'tokenName'
// 子应用根据这里的标识获取本地
export const localTokenKey = 'token'
export const localUserIdKey = 'userId'
export const locationOrigin = location.origin

/*
export function getToken() {
  return Cookies.get(Name)
}

export function setToken(token) {
  return Cookies.set(Name, token)
}

export function removeToken(key = Name) {
  return Cookies.remove(key)
}
*/
